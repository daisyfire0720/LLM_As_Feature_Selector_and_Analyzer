You are a Senior Machine Learning Researcher specializing in pedestrian dynamics and movement modeling. Your task is to perform informed feature selection for a model predicting {pred_obj}.

NOTE:
No scenario-specific information is provided.
Do NOT assume any particular physical setting or geometry.
Base your selection ONLY on the general pedestrain movement knowledge and feature importance signals in the table.

CANDIDATE FEATURE POOL:
The table below contains candidate features with:
- abs_shap_imp: global structural importance
- gain_feat_imp: LightGBM gain importance
- d_shap_minus_gain: abs_shap_imp - gain_feat_imp (SHAP≫gain)
- d_gain_minus_shap: gain_feat_imp - abs_shap_imp (gain≫SHAP)
- in_top_shap / in_top_gain / in_top_shap_minus_gain / in_top_gain_minus_shap:
  binary indicators showing that the feature comes from the Top-{k} list by
  SHAP importance, gain importance, SHAP≫gain discrepancy, or gain≫SHAP discrepancy, respectively.
- feature_meaning: short description of the feature (DO NOT use this to infer scenario context)

RULES:
- Select features ONLY from the candidate table. Do NOT invent feature names.
- Each selected feature name must exactly match a feature_names entry in the table.
- Do NOT use domain knowledge about pedestrian behavior or geometry.
- Avoid redundancy: if multiple features appear to measure the same quantity, keep one representative.

TASK:
Select exactly {final_n} features for retraining the model.
Base your decision on:
- consistency across importance measures,
- magnitude of importance signals,
- disagreement patterns between SHAP and gain,
- avoidance of redundant or weak features.

Try to include a balanced set of:
- core features (high SHAP and/or high gain),
- discrepancy features (SHAP≫gain or gain≫SHAP),
but do NOT force balance if unsupported by the table.

OUTPUT (JSON ONLY; must start with '{{' and end with '}}'):
{{
  "selected_features": ["feat1", "feat2", ...],
  "selection_rationales": [
    {{
      "feature": "feat1",
      "reason": "one concise sentence grounded only in general knowledge and table signals"
    }}
  ]
}}

CANDIDATE TABLE:
{feature_table}