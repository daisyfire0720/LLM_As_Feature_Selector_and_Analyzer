You are helping a research project on pedestrian movement modeling.

CONTEXT:
Your are given the feature importance table for predicting {pred_obj} under {scenario_name} scenario.
Your task is to perform informed feature selection for retraining a model for predicting {pred_obj} for the same {scenario_name} scenario.
Scenario description:
{scenario_context}

DATA DICTIONARY NOTE: The candidate table includes a column feature_meaning. You must use this to apply domain-specific logic regarding how pedestrians navigate and react to their environment.

CANDIDATE FEATURE POOL:
The table below contains candidate features with:
- abs_shap_imp: global structural importance
- gain_feat_imp: LightGBM gain importance
- d_shap_minus_gain: abs_shap_imp - gain_feat_imp (SHAP≫gain)
- d_gain_minus_shap: gain_feat_imp - abs_shap_imp (gain≫SHAP)
- in_top_shap / in_top_gain / in_top_shap_minus_gain / in_top_gain_minus_shap:
  binary indicators showing that the feature comes from the Top-{k} list by
  SHAP importance, gain importance, SHAP≫gain discrepancy, or gain≫SHAP discrepancy, respectively.
- feature_meaning: what the feature represents in the pedestrian domain

RULES:
Selection Constraint: Select features ONLY from the candidate table. Match feature_names exactly.
Domain Synthesis: Do not rely solely on the highest numbers. Use domain knowledge to determine if a feature’s feature_meaning is a logical causal driver for {pred_obj} movement in this specific {scenario_name} context.
No Hallucinations: Do not invent geometric constraints or pedestrian behaviors not supported by the provided description.

TASK: Select exactly {final_n} features to retrain the model, ordered from most important → least important.
Try to include a balanced set of:
- core features (high SHAP and/or high gain),
- discrepancy features (SHAP≫gain or gain≫SHAP),
but do NOT force balance if unsupported by the table.

OUTPUT (JSON ONLY; must start with '{{' and end with '}}'):
{{
  "selected_features": ["feat1", "feat2", ...],
  "selection_rationales": [
    {{
      "feature": "feat1",
      "reason": "one sentence grounded in feature_meaning, scenario_context, and table columns"
    }}
  ]
}}
CANDIDATE TABLE: {feature_table}